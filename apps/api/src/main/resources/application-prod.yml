# Production profile — AWS Lambda + DynamoDB
# All config is injected via Lambda env vars / SSM Parameter Store

dynamodb:
  endpoint:  # empty — uses real AWS DynamoDB endpoint
  table-name: ${DYNAMODB_TABLE_NAME:DrishtiKon-Main}

aws:
  region: ${AWS_REGION:ap-south-1}

logging:
  level:
    com.drishti.kon: INFO

app:
  moderators:
    emails:
      - sandeepyadav_230295@aitpune.edu.in
      - souravkushwaha_230290@aitpune.edu.in
      - sahilkumar_230298@aitpune.edu.in
      - maneeshkumar_230287@aitpune.edu.in
