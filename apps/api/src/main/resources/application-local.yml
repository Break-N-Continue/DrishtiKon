# Local development profile
spring:
  datasource:
    url: jdbc:postgresql://localhost:${DB_PORT:5433}/drishti_kon
    username: drishti
    password: password

  jpa:
    show-sql: true

logging:
  level:
    com.drishti.kon: DEBUG
